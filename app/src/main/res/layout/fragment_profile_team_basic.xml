<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="com.example.myclub.viewModel.TeamViewModel"></import>
    </data>
<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    //Inforcontact
    <LinearLayout
        android:layout_marginHorizontal="20dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/listGridData"
        android:id="@+id/inforContact"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <TextView
            android:textSize="20dp"
            android:text="Contact"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"></TextView>

        <TextView
            android:textStyle="bold"
            android:layout_weight="1"
            android:id="@+id/phone"
            android:drawableStart="@drawable/ic_baseline_phone_24_green"
            android:text="@{TeamViewModel.getInstance().teamLiveData.phone}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"></TextView>
        <TextView
            android:textStyle="bold"
            android:id="@+id/email"
            android:drawableStart="@drawable/ic_baseline_email_24_yeallow"
            android:text="@{TeamViewModel.getInstance().teamLiveData.email}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"></TextView>
        <TextView
            android:textStyle="bold"
            android:id="@+id/address"
            android:drawableStart="@drawable/ic_baseline_email_24_yeallow"
            android:text="@{TeamViewModel.getInstance().teamLiveData.address}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"></TextView>


        <TextView
            android:textSize="20dp"
            android:text="Introduce"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"></TextView>
        <TextView
            android:text="@{TeamViewModel.getInstance().teamLiveData.introduce}"
            android:textStyle="bold"
            android:layout_width="match_parent"
            android:layout_height="150dp"></TextView>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
</layout>